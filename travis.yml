language: node_js
node_js:
 - "8.11"

addons:
  chrome: stable

before_script:
  - cd front-end
  - npm install -g @angular/cli

cache:
  npm: true
  directories:
    - node_modules

script:
  - npm run test
  - ng build --prod
